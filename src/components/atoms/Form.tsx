import styled from "styled-components";

export const FormControls = styled.fieldset`
  padding: 16px;

  display: grid;
  gap: 8px;
`;

export const FormRow = styled.div`
  display: grid;
  grid-auto-flow: column;
  gap: 8px;
`;
